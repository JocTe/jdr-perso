CREATE TABLE `characters` (
  `id` INTEGER PRIMARY KEY AUTOINCREMENT,
  `name` VARCHAR,
  `type` VARCHAR,
  `description` TEXT,
  `age` INTEGER,
  `height` INTEGER,
  `weight` INTEGER,
  `sex` VARCHAR,
  `origin` TEXT
);
